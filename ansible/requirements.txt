# ansible is pre-installed in Semaphore runner container
# ansible>=2.17.0
junos-eznc>=2.7.3
requests>=2.25.0
lxml>=4.6.0
jxmlease>=1.0.1
looseversion>=1.3.0
xmltodict>=0.13.0
# ML data collection dependencies
pyarrow>=11.0.0
pandas>=1.5.0
numpy>=1.24.0,<2.0.0  # PyArrow 23.x requires NumPy 1.x (segfaults with 2.x)
